"use client";
import { useEffect, useState } from "react";
import { LifeBuoy } from "lucide-react";
type SupportRequest = { id: string; email: string; subject: string; message: string; status: "OPEN" | "IN_PROGRESS" | "CLOSED"; createdAt: string };
export default function AdminSupportPage() { const [requests, setRequests] = useState<SupportRequest[]>([]); const [loading, setLoading] = useState(true); const load = async () => { try { const response = await fetch("/api/admin/support"); if (!response.ok) throw new Error("Failed to load support requests"); setRequests(await response.json()); } catch (error) { console.error(error); } finally { setLoading(false); } }; useEffect(() => { void load(); }, []); const updateStatus = async (id: string, status: SupportRequest["status"]) => { const response = await fetch("/api/admin/support", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, status }) }); if (response.ok) void load(); }; if (loading) return <div className="grid min-h-48 place-items-center text-sm text-text-muted">Loading support requests…</div>; return <div className="mx-auto max-w-5xl"><header className="mb-6 flex items-start gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-accent-light text-accent"><LifeBuoy size={21} /></span><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-accent">Administration</p><h1 className="mt-1 font-outfit text-3xl font-bold tracking-[-0.03em] text-text">Support inbox</h1><p className="mt-1 text-sm text-text-muted">Requests sent from GemmaNote account settings.</p></div></header><div className="space-y-3">{requests.length === 0 && <div className="rounded-2xl border border-border bg-panel p-10 text-center text-sm text-text-muted">No support requests yet.</div>}{requests.map((request) => <article key={request.id} className="rounded-2xl border border-border bg-panel p-5 shadow-[0_4px_16px_rgba(30,41,59,0.025)]"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><p className="font-semibold text-text">{request.subject}</p><p className="mt-1 text-xs text-text-muted">{request.email} · {new Date(request.createdAt).toLocaleString()}</p></div><select value={request.status} onChange={(event) => void updateStatus(request.id, event.target.value as SupportRequest["status"])} className="h-10 rounded-xl border border-border bg-panel-alt px-3 text-xs font-bold text-text-muted outline-none focus:border-accent"><option value="OPEN">OPEN</option><option value="IN_PROGRESS">IN PROGRESS</option><option value="CLOSED">CLOSED</option></select></div><p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-text-muted">{request.message}</p></article>)}</div></div>; }
