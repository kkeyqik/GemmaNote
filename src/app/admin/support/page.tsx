"use client";
import { useEffect, useState } from "react";
import { LifeBuoy } from "lucide-react";
type SupportRequest = { id: string; email: string; subject: string; message: string; status: "OPEN" | "IN_PROGRESS" | "CLOSED"; createdAt: string };
export default function AdminSupportPage() { const [requests, setRequests] = useState<SupportRequest[]>([]); const [loading, setLoading] = useState(true); const load = async () => { try { const response = await fetch("/api/admin/support"); if (!response.ok) throw new Error("Failed to load support requests"); setRequests(await response.json()); } catch (error) { console.error(error); } finally { setLoading(false); } }; useEffect(() => { void load(); }, []); const updateStatus = async (id: string, status: SupportRequest["status"]) => { const response = await fetch("/api/admin/support", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, status }) }); if (response.ok) void load(); }; if (loading) return <div className="grid min-h-48 place-items-center text-sm text-slate-500">Loading support requests…</div>; return <div className="mx-auto max-w-5xl"><header className="mb-6 flex items-start gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-indigo-50 text-indigo-500"><LifeBuoy size={21} /></span><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-indigo-500">Administration</p><h1 className="mt-1 font-[Outfit] text-3xl font-bold tracking-[-0.03em] text-slate-800">Support inbox</h1><p className="mt-1 text-sm text-slate-500">Requests sent from GemmaNote account settings.</p></div></header><div className="space-y-3">{requests.length === 0 && <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center text-sm text-slate-400">No support requests yet.</div>}{requests.map((request) => <article key={request.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_4px_16px_rgba(30,41,59,0.025)]"><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><p className="font-semibold text-slate-800">{request.subject}</p><p className="mt-1 text-xs text-slate-400">{request.email} · {new Date(request.createdAt).toLocaleString()}</p></div><select value={request.status} onChange={(event) => void updateStatus(request.id, event.target.value as SupportRequest["status"])} className="h-10 rounded-xl border border-slate-200 bg-slate-50 px-3 text-xs font-bold text-slate-600 outline-none focus:border-indigo-400"><option value="OPEN">OPEN</option><option value="IN_PROGRESS">IN PROGRESS</option><option value="CLOSED">CLOSED</option></select></div><p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-slate-600">{request.message}</p></article>)}</div></div>; }
